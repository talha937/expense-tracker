# Required for JWT token signing
SECRET_KEY=replace-with-a-long-random-secret

# Optional MySQL configuration (leave empty to use SQLite fallback)
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=3306
DB_NAME=
